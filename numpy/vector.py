import numpy as np

# Create a vector (1D array)
vector = np.array([1, 2, 3, 4, 5])
print("Vector:", vector)

# Vector operations
print("Sum:", np.sum(vector))
print("Mean:", np.mean(vector))
print("Dot product with itself:", np.dot(vector, vector))
